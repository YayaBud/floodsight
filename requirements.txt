# ── Core numerics ──────────────────────────────────────────────────────────────
numpy>=1.26
scipy>=1.12

# ── Raster / vector ────────────────────────────────────────────────────────────
rasterio>=1.3
shapely>=2.0
geopandas>=0.14
pyproj>=3.6
fiona>=1.9

# ── DEM hydrology ──────────────────────────────────────────────────────────────
pysheds>=0.4

# ── Road graph ─────────────────────────────────────────────────────────────────
osmnx>=1.9
networkx>=3.2

# ── Hydrodynamics ──────────────────────────────────────────────────────────────
# Install ANUGA separately via conda or pip:
#   pip install anuga
# Install PySPH separately:
#   pip install pysph

# ── API ────────────────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# ── Charts ─────────────────────────────────────────────────────────────────────
plotly>=5.22

# ── Export ─────────────────────────────────────────────────────────────────────
simplekml>=1.3
reportlab>=4.2        # EAP PDF generation
lxml>=5.2

# ── Data I/O ───────────────────────────────────────────────────────────────────
xarray>=2024.2
zarr>=2.18
pandas>=2.2
pyarrow>=16.0         # GeoParquet / Parquet

# ── Utilities ──────────────────────────────────────────────────────────────────
tqdm>=4.66
click>=8.1
requests>=2.31
pydantic>=2.7
